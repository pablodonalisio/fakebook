<div class="card mt-4">
  <div class="card-header py-1 d-flex justify-content-between">
    <h5 class="card-title"><%= current_user.email%></h5>
    <div class="header-buttons">
      <%= link_to "Edit", root_path, class: "btn btn-outline-primary" %>
      <%= link_to "Delete", root_path, method: "delete", class: "btn btn-outline-primary" %>
    </div>
  </div>
  <div class="card-body mt-0">
    <p class="card-text"><%= current_user.email %></p>
  </div>
</div>